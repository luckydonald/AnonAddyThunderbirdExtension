Done. Created two memory files and updated the index:

- `xul_popuphidden_bubbling.md` — documents the `{ once: true }` / bubbling bug and the `e.target === popup` fix pattern
- `experiment_alias_matching.md` — documents that both popup and experiment must use description-based matching, and why
- `MEMORY.md` — two new entries appended
